apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "wso2apim.fullname" . }}-test-connection"
  labels:
    {{- include "wso2apim.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "wso2apim.fullname" . }}:{{ .Values.integration.wso2apim.service.port }}']
  restartPolicy: Never
